# Libpcap/Trippy Tauri Plugin

This contains the implementation for the self-extracting executable
`ipmap-child`, which runs as an administrator and links to `libpcap.so`
or runs basic traceroutes with [`trippy`](https://trippy.rs).
